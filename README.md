# Go Core Tasks — Решения

Этот репозиторий содержит завершённые решения учебных заданий модуля **Go Core**.  
Каждое задание расположено в отдельной директории и включает:

- исходный код (`main.go`)
- модульные тесты (`main_test.go`)
- демонстрацию работы в `main()` (если требуется)

## 📁 Структура проекта

```
go-core-task/
├── 1/
│     ├── main.go
│     └── main_test.go
├── 2/
│     ├── main.go
│     └── main_test.go
├── 3/
│     ├── main.go
│     └── main_test.go
...
├── 9/
│     ├── main.go
│     └── main_test.go
└── README.md
```

Каждая директория представляет отдельный пакет `main`.  
Файлы именованы одинаково, что соответствует стандартной практике Go.

---

# 📘 Краткое описание выполненных задач

## **Задание 1 — Типы данных, конвертация и SHA256**
Реализовано:

- объявление переменных различных типов  
- определение типа переменной  
- преобразование в строку, затем в срез рун  
- вставка соли `go-2024`  
- SHA256 хеширование  
- демонстрация в `main()`  
- unit-тесты ко всем функциям

---

## **Задание 2 — Работа со слайсами**
Выполнены функции:

- генерация случайного слайса  
- получение нового слайса из чётных чисел  
- добавление элемента  
- глубокое копирование  
- удаление элемента по индексу  
- демонстрация в `main()`  
- тестирование каждой функции

---

## **Задание 3 — StringIntMap**
Реализованы методы:

- Add  
- Remove  
- Exists  
- Get  
- Copy  

Присутствуют тесты на все операции.

---

## **Задание 4 — Разность строковых слайсов**
Функция возвращает элементы первого слайса, которых нет во втором.  
Покрыто тестами.

---

## **Задание 5 — Пересечение слайсов int**
Реализовано:

- проверка наличия пересечения  
- возврат списка пересекающихся значений  

Добавлены тесты.

---

## **Задание 6 — Генератор случайных чисел через канал**
Сделан генератор:

- через небуферизированный канал  
- с непрерывной отправкой значений  

Протестировано.

---

## **Задание 7 — Слияние нескольких каналов**
Реализована функция объединения N входных каналов в один выходной.  
Есть тесты.

---

## **Задание 8 — Собственная WaitGroup на семафоре**
Создана реализация WaitGroup, **не использующая sync.WaitGroup**, с тестами.

---

## **Задание 9 — Конвейер обработки чисел**
Готов pipeline:

1. чтение `uint8` из первого канала  
2. преобразование в `float64`  
3. возведение в куб  
4. запись во второй канал  

В `main()` демонстрация работы, есть тесты.

---

# 🧪 Тестирование

Запуск всех тестов проекта:

```
go test ./...
```

Запуск тестов конкретного задания:

```
go test ./4
```

---

# 🚀 Запуск решений

Каждое задание имеет собственный `main.go`.  
Запуск:

```
go run ./1
```

Или:

```
cd 1
go run main.go
```


